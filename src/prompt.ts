export const folderPath = ""

interface BasicInfosToWork {
    fileContent: string
    filePath: string
}

export const compiler = ({ fileContent, filePath }: BasicInfosToWork) => {
 
}

export const parser = (results: any): any => {
    
}